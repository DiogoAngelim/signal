{
  "name": "@signal/emergency-awareness",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "pnpm --filter @signal/climate-forecast build",
    "build": "vite build && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "pnpm run typecheck",
    "dev": "vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1",
    "pretypecheck": "pnpm --filter @signal/climate-forecast build",
    "test": "vitest run",
    "pretest": "pnpm --filter @signal/climate-forecast build",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@signal/climate-forecast": "workspace:*",
    "@signal/decision-memory": "workspace:*",
    "@signal/runtime": "workspace:*",
    "lucide-react": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^20.17.16",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "typescript": "^5.7.3",
    "vite": "catalog:",
    "vitest": "^2.1.9"
  }
}
