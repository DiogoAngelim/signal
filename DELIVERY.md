# Delivery

This repository now delivers Signal as a coherent protocol/runtime project:

- protocol RFCs
- JSON-schema artifacts
- Node.js reference runtime
- HTTP binding
- PostgreSQL idempotency store
- generic runnable examples
- reference server
- conformance fixtures

The repo is intentionally explicit. Signal provides contracts and primitives, not business workflows.
