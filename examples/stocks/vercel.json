{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && corepack pnpm@10.12.4 --dir src install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack pnpm@10.12.4 --dir src run build && node ./copy-public.mjs",
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "includeFiles": "src/artifacts/**",
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/",
      "destination": "/index.html"
    },
    {
      "source": "/api/(.*)",
      "destination": "/api?path=$1"
    },
    {
      "source": "/dashboard",
      "destination": "/index.html"
    },
    {
      "source": "/dashboard/(.*)",
      "destination": "/index.html"
    }
  ]
}
