{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "prepare:deploy-assets": "node ./copy-stocks-public.mjs",
    "build:artifacts": "pnpm -r --filter @workspace/api-server --filter @workspace/signal-markets --if-present run build",
    "build": "pnpm run prepare:deploy-assets && pnpm run typecheck && pnpm run build:artifacts",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
